#include "main.h"
/**
 * area - calcula el area
 * de un circulo
 * @radius: radio
 * return: area del circulo
 */
float areaC(float radius)
{
    float result = 0;
    result = 3.14 * (radius * radius);
    return (result);
}
