#include "main.h"
/**
 * area - calcula el diametro
 * de un circulo
 * @radius: radio
 * return: diametro del circulo
 */
float diameterC(float radius)
{
    float result = 0;
    result = 2 * radius;
    return (result);
}
