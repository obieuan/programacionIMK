#include "main.h"
/**
 * perimeter - calcula el perimetro
 * de un rectangulo
 * @width: ancho
 * @lenght: largo
 * return: perimetro
 */
float perimeter(float length, float width)
{
    return 2 * (length + width);
}
