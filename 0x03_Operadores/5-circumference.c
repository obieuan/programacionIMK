#include "main.h"
/**
 * area - calcula la circunferencia
 * de un circulo
 * @radius: radio
 * return: circunferencia del circulo
 */
float circumferenceC(float radius)
{
    float result = 0;
    result = 2 * 3.14 * radius;
    return (result);
}
