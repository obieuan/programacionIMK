#include "main.h"
#include <stdio.h>
/**
 * Este programa calcula el
 * módulo de un número
 * Return: 0 if succeed
 */
int main(void)
{
    int resultado = 0;
    int n1=0;
    int n2=0;

    /*Aquí va tu código*/
    resultado = mod(n1,n2);
    printf("El módulo %d de %d es: %d\n",n1,n2,resultado);
    return 0;
}
